import dagger.testing.it.BuildLogValidator;
import java.io.File;

File buildLog = new File(basedir, "build.log");
new BuildLogValidator().hasText(buildLog, new String[]{
    "No injectable members on test.Dependency.", 
	"required by test.TestApp for test.TestModule"});
